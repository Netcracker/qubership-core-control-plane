apiVersion: batch/v1
kind: Job
metadata:
  name: hey-load-test
spec:
  template:
    spec:
      containers:
      - name: hey
        image: ghcr.io/netcracker/svt-test:svt-test-snapshot
        command:
          - "/run-hey.sh"
      restartPolicy: Never